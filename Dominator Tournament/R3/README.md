# Round 3 — Semifinals

All four Round 2 results are confirmed. Both semifinals are open:

- `r3-m1`: Dominator vs Dominance
- `r3-m2`: Dominacion vs Dominate

Each matchup has its own `match.json` and blank `draft.json`. Add the new draft when supplied, then place each original race export in a numbered race-description folder. Round 2 lineups and results remain in Round 2; no Round 3 scores or selections have been assumed.

See [the match-data guide](../../docs/match-data.md).
