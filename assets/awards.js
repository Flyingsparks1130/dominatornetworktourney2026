/* Public cards stay blank. The separately generated local preview supplies standings. */
(function(global){
 'use strict';
 const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const fmt=(v,d=0)=>typeof v==='number'&&Number.isFinite(v)?v.toLocaleString('en-US',{maximumFractionDigits:d}):'—';
 const base='https://flyingsparks1130.github.io/dominatornetworktourney2026/';
 const asset=(path,options)=>options.assets?.[path]||((options.local&&path&&!path.startsWith('data:'))?base+path:path);
 const ordinal=i=>i===0?'02':'03';
 function awardCard(a,options={}){
  const revealed=Boolean(options.revealed),winner=revealed?a.winner:null;
  const pending=revealed?(a.reason||'No qualifying record yet.'):'The winner will be revealed after the tournament.';
  const value=winner?fmt(winner.value,['efficiency','blocked_seconds','duel_seconds','lane_loss_m'].includes(a.metric)?3:0):'—';
  const media=asset(a.image,options),trophy=asset(`assets/awards/trophy-${a.trophy}.png`,options);
  const player=winner&&options.standings?.players.find(p=>p.id===winner.player_id);
  const proof=player?`<details class="award-proof"><summary>See the receipts</summary><p>${esc(a.rule)}</p><p>${fmt(player.starts)} races · ${fmt(player.build_count)} fielded builds · ${esc(player.rounds.join(', '))}</p>${winner.tiebreaks.length?`<p class="award-tiebreak">Tiebreakers: ${winner.tiebreaks.map(t=>esc(t.field.replaceAll('_',' '))+': '+fmt(t.value,4)).join(' · ')}</p>`:''}<div class="award-proof-scroll"><table><thead><tr><th>Record</th><th>Evidence</th></tr></thead><tbody>${a.metric==='dqs'?player.dq_records.map(d=>`<tr><td>${esc(d.match_id.toUpperCase())}</td><td>${fmt(d.count)} DQ · ${esc(d.source)}</td></tr>`).join(''):['base_total','efficiency','guts','wisdom','sp','debuffs'].includes(a.metric)?player.builds.map(b=>`<tr><td>${esc(b.match_id.toUpperCase())}<br>${esc(b.uma)}</td><td>${a.metric==='sp'?fmt(b.sp)+' SP':a.metric==='guts'?fmt(b.stats.guts)+' Guts':a.metric==='wisdom'?fmt(b.stats.wisdom)+' Wit':a.metric==='debuffs'?b.skills.map(s=>esc(s.name)).join(', '):fmt(b.total)+' base stats'}<br><a href="${base+encodeURI(b.source)}" target="_blank" rel="noopener">Source export ↗</a></td></tr>`).join(''):player.races.map(r=>`<tr><td>${esc(r.match_id.toUpperCase())}<br>${esc(r.race_folder)}</td><td>${a.metric==='late_starts'?fmt(r.start_delay_ms,3)+' ms'+(r.start_delay_ms>=66?' · Late':''):a.metric==='failed_wit'?fmt(r.failed_wit)+' failed wit checks':a.metric==='rushed'?fmt(r.rushed)+' incidents · '+fmt(r.rushed_seconds,3)+' s':a.metric==='blocked_incidents'||a.metric==='blocked_seconds'?fmt(r.blocked_incidents)+' incidents · '+fmt(r.blocked_seconds,3)+' s':a.metric==='duel_seconds'?fmt(r.duel_seconds,3)+' s':a.metric==='lane_loss_m'?fmt(r.lane_loss_m,3)+' m':(r.place?'Place '+r.place:'Outside podium')+' · '+fmt(r.points)+' pts'}${r.url?`<br><a href="${base+r.url}" target="_blank" rel="noopener">Race evidence ↗</a>`:''}</td></tr>`).join('')}</tbody></table></div>${a.metric==='efficiency'?`<p>${fmt(player.points)} points ÷ ${fmt(player.base_total)} stats × 1,000 = ${fmt(player.efficiency,6)}</p>`:''}</details>`:'';
  return `<article class="award-card award-${esc(a.id)} ${media?'has-art':''}" data-award="${esc(a.id)}"><div class="award-art">${media?`<img src="${esc(media)}" alt="${a.id==='nitro'?'Gold Ship shrugs at another incident':a.id==='fine-motion'?'Fine Motion presents her remarkably low Wit':esc(a.name)+' artwork'}" loading="lazy">`:`<div class="award-art-placeholder"><span aria-hidden="true">✦</span><small>THE DOMINATOR HONORS</small></div>`}<span class="award-category">${esc(a.category)}</span>${options.local?`<label class="award-art-edit">Change PNG / GIF<input type="file" accept="image/png,image/gif,image/webp" data-art="${esc(a.id)}" aria-label="Change artwork for ${esc(a.name)}"></label>`:''}</div><div class="award-copy"><h3>${esc(a.name)}</h3><p class="award-description"><em>${esc(a.description)}</em></p><div class="award-winner"><img class="award-trophy" src="${esc(trophy)}" alt="Custom ${a.trophy==='nitro'?'late-start clock':a.trophy==='wit'?'Wit':'championship'} trophy"><div class="award-name"><span>${revealed?(winner?'CURRENT LEADER':a.status==='tie'?'TIE — REVIEW NEEDED':'AWAITING EVIDENCE'):'WINNER'}</span><strong>${winner?esc(winner.name):revealed?'Pending':'To be revealed'}</strong><small>${winner?esc(winner.team):revealed?esc(pending):'The suspense is part of the prize.'}</small></div><div class="award-number"><b>${value}</b><small>${esc(a.unit)}</small></div></div><details class="award-runners"><summary>Runners-up <span>${revealed?'Current contenders':'Revealed at the final'}</span></summary><ol start="2">${revealed?(a.runners_up?.length?a.runners_up.map((p,i)=>`<li><span>${ordinal(i)}</span><div><strong>${esc(p.name)}</strong><small>${esc(p.team)}</small></div><b>${fmt(p.value,['efficiency','blocked_seconds','duel_seconds','lane_loss_m'].includes(a.metric)?3:0)} <small>${esc(a.unit)}</small></b></li>`).join(''):'<li class="award-no-runner">No other qualifying contender yet.</li>'):[0,1].map(i=>`<li><span>${ordinal(i)}</span><div><strong>To be revealed</strong><small>Contender under wraps</small></div><b>—</b></li>`).join('')}</ol></details>${proof}</div></article>`;
 }
 function clubStats(index){
  const completed=index.matches.filter(m=>m.winner_id);
  const races=completed.reduce((n,m)=>n+(m.reported_results?.races.length||m.races.length),0);
  return `<div class="stats-totals"><div><b>${index.teams.length}</b><span>Clubs</span></div><div><b>${completed.length}</b><span>Completed matches</span></div><div><b>${races}</b><span>Reported races</span></div><div><b>${index.matches.filter(m=>m.status==='ready').length}</b><span>Matches ready</span></div></div><div class="stats-table-wrap"><table class="stats-table"><caption>Club records · all tournament rounds</caption><thead><tr><th>Club</th><th>Wins</th><th>Losses</th><th>Points for</th><th>Points against</th><th>Status</th></tr></thead><tbody>${index.teams.slice().sort((a,b)=>a.seed-b.seed).map(t=>{const matches=completed.filter(m=>m.participants.some(p=>p?.id===t.id));return `<tr><th><a href="club.html?id=${encodeURIComponent(t.id)}">${esc(t.name)}</a></th><td>${matches.filter(m=>m.winner_id===t.id).length}</td><td>${matches.filter(m=>m.loser_id===t.id).length}</td><td>${matches.reduce((n,m)=>n+(m.scores?.[t.id]||0),0)}</td><td>${matches.reduce((n,m)=>n+Object.entries(m.scores||{}).filter(([id])=>id!==t.id).reduce((n,[,v])=>n+v,0),0)}</td><td>${index.champion_id===t.id?'Champion':index.eliminated.includes(t.id)?'Eliminated':'In contention'}</td></tr>`}).join('')}</tbody></table></div>`;
 }
 function page(index,config,standings,options={}){
  const revealed=options.revealed&&Boolean(standings);
  const cards=revealed?standings.awards:AwardEngine.catalog(config);
  const ordered=[cards.find(a=>a.id==='nitro'),cards.find(a=>a.id==='fine-motion'),...cards.filter(a=>!['nitro','fine-motion'].includes(a.id))];
  return `<section class="awards-hero"><div><p class="awards-eyebrow">DOMINATOR NETWORK · 2026</p><h1>The awards.</h1><p>One tournament. Some very specific achievements.</p></div><span class="awards-seal">${revealed?'PRIVATE PREVIEW':'REVEAL AT THE FINAL'}</span></section>${options.local?`<aside class="preview-tools"><div><strong>Your private rehearsal</strong><p>Current standings from Round 2 onward. This file does not publish anything.</p></div><div class="preview-actions"><button id="refresh-awards" class="btn">Refresh from live data ↻</button><button id="toggle-reveal" class="btn">${revealed?'Preview blank public cards':'Show private standings'}</button><button id="download-awards" class="btn">Export standings JSON</button><label class="btn">Import verified records<input id="import-award-records" type="file" accept="application/json,.json"></label></div><p id="preview-status" role="status">${standings?`${standings.coverage.verified_races} / ${standings.coverage.expected_races} verified races · ${standings.coverage.played_builds} fielded builds · refreshed ${new Date(standings.generated_at).toLocaleString()}`:'Ready'}</p></aside>`:`<p class="awards-intro">The categories are set. Winners, numbers, and runners-up stay under wraps until the final reveal.</p>`}<section aria-label="Tournament awards" class="awards-grid">${ordered.map(a=>awardCard(a,{...options,revealed,standings})).join('')}</section><section id="tournament-statistics" class="tournament-statistics"><div class="stats-section-heading"><div><p class="awards-eyebrow">BEYOND THE TROPHIES</p><h2>Tournament statistics</h2></div><a class="btn" href="bracket.html">Open bracket ↗</a></div>${clubStats(index)}${options.local&&revealed?`<section class="player-statistics"><div class="stats-section-heading"><div><p class="awards-eyebrow">ROUND 2 ONWARD</p><h2>Player statistics</h2></div></div><div class="stats-filters"><label>Player or club<input id="stats-search" type="search" placeholder="Search trainers…"></label><label>View<select id="stats-view"><option value="results">Points & podiums</option><option value="builds">Build stats & SP</option><option value="events">Race events</option></select></label></div><div id="player-stats-table"></div><details class="stats-method"><summary>Counting rules & evidence coverage</summary><p>Each played build counts once per player per matchup. Base stats are the submitted stats before mood and race bonuses. Race results and events count for every verified race from Round 2 onward. Round 1 contributes only confirmed disqualifications.</p><p>Performance Anxiety ranks the lowest points per 1,000 base stats; Nakayama Festa ranks the highest. All other totals follow the descriptions on their cards. Equal metrics use each card’s stated tiebreakers; exact ties require one sourced organizer decision.</p><p>Blocking is credited to the affected runner. Verified failed wit checks exclude failed conditions and unresolved outcomes. SP counts full purchase prices and prerequisite tiers once, including inherited uniques; native uniques are excluded.</p><p>Duel duration and lane-change distance loss require verified observations. Missing evidence is never treated as zero. Wheelchair requires two played rounds from R2 onward.</p>${standings.issues.length?`<ul>${standings.issues.map(i=>`<li>${esc(i)}</li>`).join('')}</ul>`:''}</details></section>`:''}</section>`;
 }
 function playerTable(standings,query='',view='results'){
  const columns={results:[['starts','Races'],['points','Points'],['firsts','1st'],['seconds','2nd'],['thirds','3rd']],builds:[['build_count','Builds'],['base_total','Base stats'],['guts','Guts'],['wisdom','Wit'],['sp','SP']],events:[['late_starts','Late starts'],['rushed','Rushed'],['failed_wit','Wit failures'],['blocked_incidents','Blocked count'],['blocked_seconds','Blocked seconds']]}[view];
  const rows=standings.players.filter(p=>p.starts>0&&(!query||(p.name+' '+p.team).toLowerCase().includes(query.toLowerCase()))).sort((a,b)=>b[columns[1][0]]-a[columns[1][0]]||a.name.localeCompare(b.name));
  return `<div class="stats-table-wrap"><table class="stats-table"><thead><tr><th>Player</th><th>Club</th><th>Rounds</th>${columns.map(([,name])=>`<th>${name}</th>`).join('')}</tr></thead><tbody>${rows.map(p=>`<tr><th>${esc(p.name)}</th><td>${esc(p.team)}</td><td>${esc(p.rounds.join(', '))}</td>${columns.map(([field])=>`<td>${fmt(p[field],field==='blocked_seconds'?3:0)}</td>`).join('')}</tr>`).join('')||`<tr><td colspan="8">No matching players.</td></tr>`}</tbody></table></div>`;
 }
 async function getJSON(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw Error(`HTTP ${r.status}: ${url}`);return r.json();}
 async function publicMount(){
  const root=document.getElementById('awards-root');
  try{const [index,config]=await Promise.all([getJSON('data/tournament-index.json'),getJSON('config/awards.json')]);root.innerHTML=page(index,config,null,{revealed:false});}
  catch(e){root.innerHTML=`<div class="dt-error">The Stats page could not load. Please refresh.<p>${esc(e.message)}</p></div>`;}
 }
 function previewMount(seed){
  let {index,config,standings,skills,assets}=seed,revealed=true;
  const root=document.getElementById('awards-root');
  function render(){
   root.innerHTML=page(index,config,standings,{local:true,revealed,assets});
   root.querySelector('#toggle-reveal').onclick=()=>{revealed=!revealed;render();};
   root.querySelector('#download-awards').onclick=()=>{const blob=new Blob([JSON.stringify(standings,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='dominator-award-standings-private.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);};
   root.querySelector('#refresh-awards').onclick=refresh;
   root.querySelector('#import-award-records').onchange=async e=>{
    try{const file=e.target.files[0];if(!file)return;const imported=JSON.parse(await file.text());if(!Array.isArray(imported.verified_metrics)&&!imported.tie_decisions)throw Error('Expected verified_metrics or tie_decisions.');config={...config,verified_metrics:imported.verified_metrics??config.verified_metrics,tie_decisions:imported.tie_decisions??config.tie_decisions};await refresh();}
    catch(err){root.querySelector('#preview-status').textContent=err.message;}
   };
   for(const input of root.querySelectorAll('[data-art]'))input.onchange=()=>{const file=input.files[0];if(!file)return;const reader=new FileReader();reader.onload=()=>{const id=input.dataset.art;config.images[id]=reader.result;const award=standings.awards.find(a=>a.id===id);if(award)award.image=reader.result;render();};reader.readAsDataURL(file);};
   if(revealed){const draw=()=>{root.querySelector('#player-stats-table').innerHTML=playerTable(standings,root.querySelector('#stats-search').value,root.querySelector('#stats-view').value);};root.querySelector('#stats-search').oninput=draw;root.querySelector('#stats-view').onchange=draw;draw();}
  }
  async function refresh(){
   const button=root.querySelector('#refresh-awards'),status=root.querySelector('#preview-status');button.disabled=true;status.textContent='Reading current tournament records…';
   try{
    const [nextIndex,nextSkills]=await Promise.all([getJSON(base+'data/tournament-index.json'),getJSON(base+'assets/award-skills.json')]);
    const files=nextIndex.matches.filter(m=>Number(m.round.slice(1))>=(config.performance_round_min??2)).flatMap(m=>m.races),docs={};
    let completed=0;
    // Small batches keep refresh responsive and avoid flooding the host.
    for(let i=0;i<files.length;i+=4){await Promise.all(files.slice(i,i+4).map(async f=>{docs[f.id]=await getJSON(base+f.data_file);completed++;}));status.textContent=`Loaded ${completed} / ${files.length} race exports…`;}
    const computed=AwardEngine.compute(nextIndex,docs,config,nextSkills.skills);index=nextIndex;skills=nextSkills.skills;standings=computed;render();
   }catch(e){status.textContent='Refresh failed; the saved snapshot is still available. '+e.message;button.disabled=false;}
  }
  render();
 }
 global.AwardUI={awardCard,page,playerTable,publicMount,previewMount};
 if(typeof module!=='undefined'&&module.exports)module.exports=global.AwardUI;
})(typeof window!=='undefined'?window:globalThis);
